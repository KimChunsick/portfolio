"use strict";

const settings = [{
    // 소리 관련
    'volume': {
        'bgm': 1.0,
        'voice': 1.0,
        'sfx': 1.0
    },

    // 자동으로 진행
    'auto': {
        enable: true,
        speed: 1.0
    },

    // 대사 출력 속도
    'print-speed': 10,
}];

export default settings;
