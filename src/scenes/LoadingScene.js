import Component from '@/components/common/component'

class LoadingScene extends Component {
  constructor() {
    super('LoadingScene')
  }

  mount() {
    super.mount()
  }

  render() {
    return super.render('<p>loading</p>')
  }
}

export default LoadingScene
